@props(['article'])
<article class="news-card"><a href="{{ route('news.show', $article->slug) }}">@if($article->image_path)<img class="news-image" src="{{ Storage::disk('public')->url($article->image_path) }}" alt="" loading="lazy">@else<div class="news-placeholder" aria-hidden="true"><span>KABAR KOPERASI</span><strong>Bergerak<br>bersama. ↗</strong></div>@endif<div class="news-card-body"><div class="news-meta"><span>{{ $article->category }}</span><time datetime="{{ $article->published_at->toIso8601String() }}">{{ $article->published_at->translatedFormat('d M Y') }}</time></div><h3>{{ $article->title }}</h3><p>{{ $article->excerpt }}</p><span class="text-link">Baca cerita <span aria-hidden="true">↗</span></span></div></a></article>
